{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test.js",
    "setup": "npm install && echo Setup complete! && echo 1. Add MongoDB URI to .env && echo 2. Add serviceAccountKey.json to config/ folder && echo 3. Run: npm start"
  }
}
